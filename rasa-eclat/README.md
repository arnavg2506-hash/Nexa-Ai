# Rasa Eclat

This Vite storefront was moved out of the repository root so it can coexist with the NEXA AI Next.js application without replacing NEXA's build configuration.

Run it independently:

```bash
cd rasa-eclat
npm install
npm run dev
```
